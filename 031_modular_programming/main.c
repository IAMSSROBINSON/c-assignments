// write a program to ask the user their username and password
// when entered tell the user they are logged in and confirm their details
// ask the user if they want to logout
// if the string "YES" it matched then confirm with a message
// if not matched then state they are still logged in in a message
// export this functions using modular programming principles

#include <stdio.h>
#include <string.h>
#include "user.h"


int main (int argc, char *argv[]) {

	formData();
	logout();


	return 0;
}
